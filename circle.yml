machine:
  ruby:
    version: 2.3.3
checkout:
  post:
    - bundle install
    - bash automated_build.sh
dependencies:
  override:
    - echo "Intentionally overriding dependencies phase with an echo."
test:
  override:
    - echo "Intentionally overriding tests with an echo."
general:
  branches:
    only:
      - gh-pages-ci
